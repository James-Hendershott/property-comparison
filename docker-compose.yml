version: "3.8"
services:
  property-comparison:
    build: .
    container_name: homes-shottsserver
    ports:
      - "3080:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
